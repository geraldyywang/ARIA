import { StyleSheet, View } from "react-native";
import { Card, Text } from "react-native-paper";

const HistoryCard = ({ title, highlight, date, style, mode }) => {
  return <View>Hi</View>;
};

export default HistoryCard;

const styles = StyleSheet.create({
  card: {
    color: "black",
    fontWeight: "bold",
  },
  text: {
    color: "black",
  },
});
